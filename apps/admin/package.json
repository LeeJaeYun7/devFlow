{
  "name": "@lia/admin",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "deploy": {
        "dependsOn": [
          "build"
        ],
        "executor": "nx:run-commands",
        "options": {
          "command": "bash apps/admin/deploy.sh"
        }
      }
    }
  }
}
